# RanakEngine2

A game engine built with a using C++ with a Lua frontend. Scenes, entity Components and Systems are written using Lua with access to internal C++ bindings.

## Usage
RE2 compiles to a static library that can be used in other projects, simply include as a submodule or compile the library yourself with CMake.
